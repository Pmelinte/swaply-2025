# Batch 29 scope note

This batch intentionally stays test-only for application behavior.

It does not implement the missing `/items` legacy redirect because that should be a separate, focused route patch.

The goal is to make route regressions visible before moving into larger functional flows.

# Known gap: legacy items redirect

Production currently does not resolve `/en/items` to `/en/objects`.

Batch 29 records this as a `test.fixme` because it is a test-only batch.

Batch 30 should implement the redirect and activate the test.

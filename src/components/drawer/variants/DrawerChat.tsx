"use client";

interface Props {
  conversationId: string;
}

export default function DrawerChat({ conversationId }: Props) {
  return (
    <div className="p-4 text-sm text-zinc-500 dark:text-zinc-400">
      TODO: migrate from ChatDrawer.tsx (conv: {conversationId})
    </div>
  );
}

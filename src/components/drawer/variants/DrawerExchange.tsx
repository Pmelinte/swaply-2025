"use client";

interface Props {
  swapId: string;
}

export default function DrawerExchange({ swapId }: Props) {
  return (
    <div className="p-4 text-sm text-zinc-500 dark:text-zinc-400">
      TODO: migrate from ExchangeDrawer.tsx (swap: {swapId})
    </div>
  );
}

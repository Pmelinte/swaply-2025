export const metadata = {
  title: "List a Service — Swaply",
  description: "Offer your services in exchange on Swaply.",
};

export default function NewServiceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export const metadata = {
  title: "List a Property — Swaply",
  description: "List your property for vacation exchange on Swaply.",
};

export default function NewPropertyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

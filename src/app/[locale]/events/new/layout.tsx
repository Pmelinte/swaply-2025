export const metadata = {
  title: "List an Event — Swaply",
  description: "List events, tickets, and experiences for exchange on Swaply.",
};

export default function NewEventLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

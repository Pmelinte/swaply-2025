## Swaply Next.js app

A fresh Next.js (App Router + TypeScript) bootstrap.

## How to run

```bash
# install dependencies
npm ci

# start dev server
npm run dev

# production build
npm run build
```

The app runs on [http://localhost:3000](http://localhost:3000).
